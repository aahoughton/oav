{
  "name": "@oav/spec",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: OpenAPI spec loader, $ref resolver, overlay merger for oav. Not published — consume via `oav/spec`.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@oav/core": "workspace:*",
    "@oav/schema": "workspace:*",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
