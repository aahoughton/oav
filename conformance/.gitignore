node_modules/
JSON-Schema-Test-Suite/
Overlay-Specification/
# json-schema-results.json and overlay-results.json are committed
# baselines that the respective runners compare against in CI with
# --check-baseline. The --optional variant of the JSON Schema suite
# is still gitignored — we don't track that baseline because the
# optional suite is a moving target we don't claim conformance
# against.
json-schema-results-with-optional.json
openapi-results.json
