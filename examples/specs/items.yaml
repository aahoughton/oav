openapi: "3.1.0"
info:
  title: Bulk
  version: "1"
paths:
  /items:
    post:
      summary: Bulk-create items
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                required: [id]
                properties:
                  id:
                    type: integer
      responses:
        "201":
          description: created
